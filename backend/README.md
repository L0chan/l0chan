# Backend

This folder contains the Flask backend code.

Run from the project root:

```powershell
python backend\app.py
```

The app serves HTML, CSS, JavaScript, images, and uploads from `frontend/`.
