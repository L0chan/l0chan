# Frontend

This folder contains the Flask frontend:

- `templates/` contains HTML pages.
- `static/` contains CSS, JavaScript, images, uploads, and other assets.

The backend app is configured to serve templates and static files directly from this folder.
